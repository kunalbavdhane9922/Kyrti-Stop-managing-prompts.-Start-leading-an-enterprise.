/**
 * V1 Feature Flags
 * Controls which UI modules are visible in the application.
 */
export const FEATURES = {
  TREASURY: false,
  SPATIAL_WORKSPACE: false,
  COMMAND_CENTER: false,
  MEMORY_VAULT_METRICS: false,
  AUDIT_TIMELINE: false,
  
  // Active V1 Features
  MARKETPLACE: true,
  WORKFORCE: true,
  GOVERNANCE: true,
  IDENTITY: true,
  COMPANY: true,
  MEMORY_SEARCH: true,
};
